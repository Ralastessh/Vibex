# 머신마다 다른 값만 여기에 둔다. 경로는 적지 않는다 —
# 작업공간/설정 파일 경로는 체크아웃 위치에서 자동으로 계산된다.
BRIDGE_DEVICE_TOKEN=change-me

# 필요할 때만 사용하는 선택 항목:
# BRIDGE_WORKSPACE_ROOT=/절대/경로   # test-projects 를 저장소 밖에 둘 때만
# BRIDGE_OPENAI_API_KEY=sk-...
